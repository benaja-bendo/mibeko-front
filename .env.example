MIBEKO_FRONT_HOST=

VITE_LARAVEL_API_URL=http://localhost:8000/api/v1

VITE_PYTHON_API_URL=http://localhost:8001/api/v1

# Analytics Umami (optionnel). Si les DEUX variables sont définies au build,
# le script Umami est injecté dans index.html ; sinon, aucun script n'est ajouté.
# L'origine doit aussi être autorisée par la CSP nginx (docker/nginx/default.conf).
# VITE_UMAMI_URL=https://stats.mibeko.fr
# VITE_UMAMI_WEBSITE_ID=